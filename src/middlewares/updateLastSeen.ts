import { TController } from '../types'
import { UserModel } from '../models'

export const updateLastSeen: TController = (req, res, next) => {
    //! сделать update last seen UserModel.updateOne()
    next()
}
